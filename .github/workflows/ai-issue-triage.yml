name: AI Issue Triage

on:
  issues:
    types: [opened, labeled]

permissions:
  contents: read
  issues: write
  models: read  # Required for GitHub Models

jobs:
  triage:
    runs-on: ubuntu-latest

    steps:
      - name: AI Issue Triage
        uses: cajias/custom-github-actions/ai-triage@copilot/fix-issue-for-custom-actions
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
